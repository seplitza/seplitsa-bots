# 🤖 Telegram File ID Bot - Инструкция

## Описание

Полноценный Telegram-бот для получения file_id любых медиа-файлов.

## Возможности

✅ **Поддерживаемые типы медиа:**
- 📹 Видео (video)
- 🖼 Фото (photo)
- 📄 Документы (document)
- 🎵 Аудио (audio)
- 🎤 Голосовые сообщения (voice)
- ⭕️ Круглые видео (video_note)

✅ **Функции:**
- Получение file_id для базы знаний
- Информация о файле (размер, длительность, разрешение)
- Статистика использования
- Готовый формат для копирования в JSON

## Локальный запуск

```bash
# Перейдите в директорию проекта
cd /Users/alexeipinaev/Documents/seplitsa-bots

# Запустите бота
python3 scripts/get_video_file_id.py
```

## Установка на сервер (опционально)

### Вариант 1: Ручной запуск

```bash
# На сервере
cd /home/seplitsa/seplitsa-bots
source venv/bin/activate
python3 scripts/get_video_file_id.py
```

### Вариант 2: Как systemd сервис

```bash
# Скопируйте сервис-файл
sudo cp systemd/file-id-bot.service.template /etc/systemd/system/file-id-bot.service

# Перезагрузите systemd
sudo systemctl daemon-reload

# Запустите бот
sudo systemctl start file-id-bot

# Включите автозапуск
sudo systemctl enable file-id-bot

# Проверьте статус
sudo systemctl status file-id-bot

# Просмотр логов
sudo journalctl -u file-id-bot -f
```

## Использование

### 1. Запустите бота

Найдите бота в Telegram: **@seplitza_info_bot** (или ваш токен)

### 2. Отправьте команды

```
/start  - Приветствие и инструкции
/stats  - Статистика использования
/format - Формат для базы знаний
/help   - Справка
```

### 3. Получите file_id

1. **Перешлите медиа-файл боту** (видео, фото, документ и т.д.)
2. **Получите file_id** и информацию о файле
3. **Скопируйте file_id** из ответа

### Пример ответа бота:

```
✅ VIDEO FILE ID ПОЛУЧЕН!

📹 File ID:
BAACAgIAAxkBAAICxmZK5z8AAV...

ℹ️ Информация:
• Размер: 5.23 MB
• Длительность: 45 сек
• Разрешение: 1920x1080

📋 Для базы знаний:
[VIDEO:BAACAgIAAxkBAAICxmZK5z8AAV...]
```

### 4. Используйте в базе знаний

Скопируйте маркер `[VIDEO:file_id]` и вставьте в `knowledge.json`:

```json
{
  "ваша тема": "[VIDEO:BAACAgIAAxkBAAICxmZK5z8AAV...]\n\nОписание..."
}
```

## Команды управления

### Запуск/Остановка (если установлен как сервис)

```bash
# Запустить
sudo systemctl start file-id-bot

# Остановить
sudo systemctl stop file-id-bot

# Перезапустить
sudo systemctl restart file-id-bot

# Проверить статус
sudo systemctl status file-id-bot
```

### Просмотр логов

```bash
# Последние 50 строк
sudo journalctl -u file-id-bot -n 50

# Отслеживание в реальном времени
sudo journalctl -u file-id-bot -f

# За сегодня
sudo journalctl -u file-id-bot --since today
```

## Возможные проблемы

### Бот не запускается

**Проблема:** `TELEGRAM_BOT_TOKEN_INFO not found`

**Решение:** Проверьте файл `.env`:
```bash
cat .env | grep TELEGRAM_BOT_TOKEN_INFO
```

### Бот не отвечает

**Решение:**
1. Проверьте что бот запущен: `ps aux | grep get_video_file_id`
2. Проверьте логи: `journalctl -u file-id-bot -n 50`
3. Перезапустите: `systemctl restart file-id-bot`

### file_id не работает в другом боте

**Причина:** file_id привязан к конкретному боту

**Решение:** Используйте тот же токен бота (TELEGRAM_BOT_TOKEN_INFO)

## Технические детали

- **Язык:** Python 3
- **Библиотека:** pyTelegramBotAPI (telebot)
- **Зависимости:** см. `requirements.txt`
- **Логирование:** systemd journal (если запущен как сервис)
- **Ограничения памяти:** 200MB (настраивается в сервисе)

## Полезные ссылки

- Основная документация: `docs/video-knowledge-base.md`
- Быстрый старт: `docs/video-quick-start.md`
- Примеры: `data/video_examples.json`

---

**Дата:** 12 декабря 2025  
**Версия:** 2.0  
**Статус:** ✅ Готов к использованию
