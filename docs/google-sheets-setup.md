# Настройка Google Sheets интеграции для Seplitsa Bot

## 🎯 Цель
Автоматическое сохранение данных о подписчиках в Google Sheets таблицу для удобного анализа и работы.

## 📋 Пошаговая инструкция

### Шаг 1: Создание Google Cloud проекта

1. **Перейдите в Google Cloud Console**: https://console.cloud.google.com/
2. **Создайте новый проект**:
   - Нажмите на выпадающее меню проекта вверху
   - Нажмите "NEW PROJECT"
   - Название: `Seplitsa Bot Integration`
   - Нажмите "CREATE"

### Шаг 2: Включение API

1. **Включите Google Sheets API**:
   - В меню слева: "APIs & Services" → "Library"
   - Поиск: "Google Sheets API"
   - Нажмите на API и "ENABLE"

2. **Включите Google Drive API** (для создания файлов):
   - Поиск: "Google Drive API" 
   - Нажмите на API и "ENABLE"

### Шаг 3: Создание Service Account

1. **Создайте Service Account**:
   - "APIs & Services" → "Credentials"
   - "CREATE CREDENTIALS" → "Service account"
   - Service account name: `seplitsa-bot-service`
   - Service account ID: `seplitsa-bot-service` (автоматически)
   - Description: `Service account for Seplitsa bots Google Sheets integration`
   - Нажмите "CREATE AND CONTINUE"

2. **Настройте роли** (необязательно для базового использования):
   - Можно пропустить, нажав "CONTINUE"
   - Нажмите "DONE"

### Шаг 4: Создание ключей

1. **Создайте JSON ключ**:
   - Найдите созданный Service Account в списке
   - Нажмите на email Service Account
   - Вкладка "KEYS"
   - "ADD KEY" → "Create new key"
   - Выберите тип: **JSON**
   - Нажмите "CREATE"
   - Файл автоматически скачается

### Шаг 5: Установка файла на сервер

1. **Переименуйте скачанный файл** в `seplitsa-credentials.json`

2. **Загрузите на сервер**:
   ```bash
   scp /path/to/seplitsa-credentials.json root@37.252.20.170:/home/seplitsa/seplitsa-bots/data/
   ```

   Или создайте файл прямо на сервере:
   ```bash
   ssh root@37.252.20.170
   nano /home/seplitsa/seplitsa-bots/data/seplitsa-credentials.json
   # Вставьте содержимое скачанного JSON файла
   ```

### Шаг 6: Тестирование

1. **Перезапустите бота**:
   ```bash
   ssh root@37.252.20.170 'systemctl restart seplitsa-info-bot'
   ```

2. **Протестируйте интеграцию**:
   - В боте отправьте команду `/test_sheets`
   - Или нажмите кнопку "📊 Тест Google Sheets"

3. **Проверьте результат**:
   - Должно появиться сообщение "✅ Тест Google Sheets успешен!"
   - В Google Drive появится таблица "Сеплица - База подписчиков"

## 📊 Структура таблицы

Таблица будет содержать следующие колонки:

| Колонка | Описание |
|---------|----------|
| Дата регистрации | Когда пользователь впервые написал боту |
| ID пользователя | Telegram ID |
| Username | @username в Telegram |
| Имя | Имя пользователя |
| Фамилия | Фамилия пользователя |
| Возраст | Из анкеты |
| Пол | Из анкеты |
| Посещение спортзала | Из анкеты |
| Частота тренировок | Из анкеты |
| Тип телефона | Из анкеты |
| Финансовый статус | Из анкеты |
| Уровень мотивации | Из анкеты |
| Текущее звание | Прогресс в системе |
| Язык | Язык интерфейса |
| Источник | "Telegram Bot" |
| Последняя активность | Дата последнего обновления |

## 🔧 Команды для работы

- `/subscribers` - показать последних подписчиков из файла
- `/test_sheets` - протестировать Google Sheets интеграцию
- Кнопка "👥 Подписчики" - в меню автора
- Кнопка "📊 Тест Google Sheets" - в меню автора

## ⚠️ Важные моменты

1. **Безопасность**: Файл `seplitsa-credentials.json` содержит приватные ключи. Не делитесь им!

2. **Права доступа**: Service Account автоматически создает таблицы в Google Drive. Если хотите дать доступ себе:
   ```python
   # Добавьте в код после создания таблицы:
   sheet.share('your-email@gmail.com', perm_type='user', role='writer')
   ```

3. **Лимиты**: Google Sheets API имеет лимиты на количество запросов. Для обычного использования это не проблема.

## 🐛 Устранение неполадок

### "Файл учетных данных Google Sheets не найден"
- Проверьте путь к файлу: `/home/seplitsa/seplitsa-bots/data/seplitsa-credentials.json`
- Проверьте права доступа: `chmod 600 seplitsa-credentials.json`

### "SpreadsheetNotFound" или подобные ошибки
- Убедитесь что включены Google Sheets API и Google Drive API
- Проверьте что JSON файл корректный (валидный JSON)

### Таблица не создается
- Проверьте что у Service Account есть права на создание файлов в Google Drive
- Попробуйте создать таблицу вручную и дать доступ Service Account

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи бота: `journalctl -u seplitsa-info-bot -f`
2. Запустите тест: `/test_sheets`
3. Проверьте файл credentials и права доступа